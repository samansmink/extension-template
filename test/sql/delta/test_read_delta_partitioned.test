# name: test/sql/delta/test_read_delta.test
# description: test creating a hive table
# group: [sql]

require httpfs

require aws

require iceberg

require delta

require glue

statement ok
CREATE SECRET (
  TYPE S3,
  PROVIDER credential_chain,
  REGION 'eu-central-1',
  PROFILE 'personal'
);

statement ok
attach '984506134640' as my_datalake (
    TYPE glue
);

query II
SELECT * FROM my_datalake.default.delta_partitioned_table ORDER BY ALL;
----
1	1
2	2
3	1
4	2
